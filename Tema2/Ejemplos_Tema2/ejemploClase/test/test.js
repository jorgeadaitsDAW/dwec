describe('#sumar', function() { 
    it('debe devolver la suma de los dos valores introducidos', 
        function() {
            expect(sumar(1,1)).to.equal(2);
    });
    it('debe devolver la suma de los dos valores introducidos', 
        function() {
            expect(sumar(1,1)).to.equal(3);
    });
});